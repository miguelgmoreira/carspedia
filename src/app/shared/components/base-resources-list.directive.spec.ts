import { BaseResourcesListDirective } from './base-resources-list.directive';

describe('BaseResourcesListDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseResourcesListDirective();
    expect(directive).toBeTruthy();
  });
});
